# G1 artifacts are digest-covered as the exact bytes of the files
# (artifact_digests in tests/approvals/g1.yaml and the SR-38 working-tree check).
# EOL conversion would make digests differ in the clone, so perform no conversion at all.
* -text
